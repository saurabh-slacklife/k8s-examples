apiVersion: v1
kind: Service
metadata:
  creationTimestamp: null
  labels:
    app: svc-py-protobuf-rest-api
    tier: backend
    type: API
    sub-type: REST
    environment: development
  name: svc-py-proto
#  namespace: py-proto
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 5000
  selector:
    app: py-protobuf-rest-api
    tier: backend
    type: API
    sub-type: REST
    environment: development
  type: NodePort
status:
  loadBalancer: {}
